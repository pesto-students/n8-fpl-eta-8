import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
  profileIcon: {},
  toolbar: {
    display: "flex",
    justifyContent: "space-between",
  },
  link: {
    textDecoration: "none",
  },
});