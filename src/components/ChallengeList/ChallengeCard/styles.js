import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    root:{
        padding:'1.5rem'
    },
    challengeDetails:{
        marginBottom:"1rem"
    },
    cardButton:{
        justifyContent:'start'
    },
});