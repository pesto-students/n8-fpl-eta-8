import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles({
    root:{
        padding:".75rem"
    },
    sensexChartCard:{
        height:'60vh',
        padding:'.25rem'
    },
    challengeFilterCard:{
        padding:'1rem',
    },
});